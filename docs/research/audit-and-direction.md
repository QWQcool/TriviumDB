# N1–N4 结果 + 旧结论审查 + 新方向 + 论文可行性

> **日期**：2026-09-18 ｜ **分支**：`research/quiver2-pipnn-rabitq-tsng` ｜ **`src/` 零改动**
> **性质**：执行 N1–N4（HANDOFF §5）后对**全部已有结论做一次对抗性审查**，并给出方向与可行性判断。
> ⚠️ 论文数字来自 arXiv `2605.02171v3` 的 HTML/摘要（抓取转述），正式写作前须与 PDF 逐格复核。

---

## 0. 执行摘要

| 项 | 结论 |
|---|---|
| **N1** | ✅ **CLIP 档补上**（`wolt_clip` 71.48% @ef=64 vs 论文 70.68% ⇒ **Δ+0.80pp**）⇒ 跨口径校准 **7/12 行**。另把**去均值修复推广到全部四档**（含一个完美的"无操作对照"） |
| **N2** | ❌ **U14 被推翻**：glove100 的 GT **没有问题**（前 1000 查询 100.00% 一致）。P2 的 84.85% 是**我自己的子采样伪影**（S=1M 只占基底 84.49%） |
| **N3** | ✅ **强阳性**：论文把 compatibility test 定义在 §6，**未指定 BQ 度量**、**未说明样本来源**；且**明确以"zero preprocessing"为卖点**、对失败数据的建议只是"改用 float32" ⇒ 我们的"去均值修复"落在其**空白处** |
| **N4** | ⚠️ **最重要的负面结果**：补上 CLIP 档与可用档的竞品曲线后，**QuIVer 在 4 个有竞品数据的档位里只有 1 个赢**（仅 cohere-768）。另发现 wolt_clip 上**连 `faiss_exact` 都只有 90.09%**（密集并列 ⇒ 召回上限被并列锁死） |
| **审查** | 8 条旧结论逐条裁决：**4 条成立、2 条需修正、2 条撤回**（§2） |
| **方向** | 推荐"**可部署性判据 + 修复**"（§3-D1），而非继续找新预测器 |
| **可行性** | **值得继续，但只到 Tier A**（arXiv 技术报告 + 上游 issue/PR）：1–2 周、有界；**不建议**追顶会（机制已发表、我们的预测器失败、修复属实现级） |
| **D1**（后补） | ✅ 判据链建成并验证 22 臂（21 臂一致）：**符号平面熵 `sign_info` 是"是否可修复"的分诊量**（0.000 vs ≥0.597 宽间隙）；★ 发现论文默认的**加权**口径在 Random-Sphere 上给 **53.2% "兼容"** 而实测召回 **0.91%**（假阳性），**取双度量最弱恰修掉它** ⇒ `t2-deployability-gate.md` |
| **D2**（后补） | ⚠️ **修正 A9/A11**：去均值把 **CLIP 档从"严格支配"推入"曲线相交"**（≥89% 高召回区 QuIVer 快 **2.3–3.9×**）；glove100c 仍被支配，gist960c 未测 ⇒ §9 |

---

## 1. N1 — 补数据集 + 把"去均值修复"推广到全部档位

### 1.1 跨口径校准：**7/12 行**

| 数据集 | dim | 我们 @ef=64 | 论文 Table 11 | Δ |
|---|---|---|---|---|
| Cohere-768 | 768 | 94.63% | 95.13% | −0.50 |
| GloVe-100 | 100 | 32.82% | 32.08% | +0.74 |
| SIFT-128 | 128 | 15.77% | 14.85% | +0.92 |
| GIST-960 | 960 | 2.10% | 2.01% | +0.09 |
| Synthetic-LR | 768 | 43.60% | 41.76% | +1.84 |
| Random-Sphere | 768 | 0.48% | 0.40% | +0.08 |
| **Wolt-CLIP-512**（本轮新增） | 512 | **71.48%** | **70.68%** | **+0.80** |

⇒ 7 行中 6 行 ≤±1.0pp。**Harness 级可信**。
（缺 5 行：`minilm-384` / `redcaps-512` / `bge-m3-1024` / `dbpedia-1536` / `dbpedia-3072`；
**`redcaps-1m` 不在本仓 `prepare_all.py` 注册表里** ⇒ 是否可复现未知。）

### 1.2 ★★ 去均值修复的**全档位**效果（含"无操作"对照）

`x' = normalize(x − mean(normalized train))`，GT 按 centered-cosine 重算。`‖μ‖` 在归一化数据上测得。

| 论文档位 | 数据集 | ‖μ‖ | **@ef=64** | Δ | **@ef=128** | Δ | @ef=1024 | Δ |
|---|---|---|---|---|---|---|---|---|
| 崩塌 <15% | **GIST-960** | 0.868 | 2.10% → **39.74%** | **+37.6** | 2.81 → **51.96** | +49.2 | 4.38 → **79.44** | +75.1 |
| 崩塌 | **SIFT-128** | — | 15.77% → **30.64%** | **+14.9** | 21.88 → 44.16 | +22.3 | 47.23 → 85.21 | +38.0 |
| 崩塌 | **Random-Sphere** | **0.001** | 0.48% → 0.51% | **+0.03** | 0.91 → 1.11 | +0.20 | 6.46 → 6.58 | +0.12 |
| 可用 32–42% | **GloVe-100** | 0.354 | 32.82% → **36.11%** | **+3.3** | 45.60 → 49.01 | +3.4 | 71.69 → 73.28 | +1.6 |
| 可用 | Synthetic-LR | 0.204 | 43.60% → 43.89% | +0.3 | 59.08 → 60.28 | +1.2 | 92.52 → 94.69 | +2.2 |
| 较高 71–78% | **Wolt-CLIP-512** | 0.797 | 71.48% → **76.81%** | **+5.3** | 77.73 → 82.19 | +4.5 | 86.81 → 89.14 | +2.3 |
| 竞争 >88% | **Cohere-768** | 0.834 | 94.63% → 93.48% | **−1.2** | 97.51 → 97.25 | −0.3 | 99.78 → 99.86 | +0.1 |

**三条读法**：
1. **无操作对照完美**：`Random-Sphere` 的 `‖μ‖=0.001`（本来就各向同性）⇒ Δ=**+0.03pp/+0.20pp**
   ⇒ **去均值流水线本身不引入任何伪影**（这是该修复可信的关键守卫）。
2. **收益与"符号平面原本是否死掉"相关，而非只与 ‖μ‖ 相关**：
   Cohere 的 `‖μ‖=0.834`（与 GIST 的 0.868 同级）却 **−1.2pp**（中性/微降），
   因为它的坐标**本来就有 41% 为负**（符号位活着）；GIST 是 **0% 为负**（符号位全死）。
   ⇒ 判据应是**逐坐标符号平衡**（或 `pos` 面 Hamming 率），`‖μ‖` 只说明"移位量"。
3. **去均值不是只修崩塌**：CLIP 档 **+5.3pp**、GloVe 档 **+3.3pp**，
   即论文归因为"modality gap 破坏全局各向同性"的中间档，**去均值直接移除那个一阶项**。

### 1.3 本轮踩到并修掉的一个真 bug（`‖mean‖=11.5`）
`cohere_train.f32` **在磁盘上未归一化**，而 `derive()` 是"先归一化再 transform" ⇒
第一版直接把**原始数据的均值**（‖μ‖=11.5）减到单位向量上，得到 `随机对 cos = 0.9973` 的退化数据。
**守卫 `‖μ‖ ≤ 1`（单位向量均值的范数不可能 >1）当场抓住**。
⇒ 已改为 `streaming_mean(normalize=True)` 并加硬断言。
**注**：gist960/sift128 的磁盘文件本来就是归一化的 ⇒ **既有的 `gist960c`/`sift128c` 结论不受影响**。

---

## 2. N2 — **U14 撤回**：glove100 的 GT 没有问题

| 检查 | 结果 |
|---|---|
| 磁盘 `glove100_{train,test}` 行范数 | 全为 **1.0000**（已归一化） |
| 原生 angular GT vs 我们重算的 cosine top-10（前 1000 查询） | **100.00%**（原始内积口径也是 100.00%） |
| 原生 GT 落在 cosine **top-100** 内的比例 | **100.00%** |
| 用 **S=1,000,000（占基底 84.49%）** 子采样后的"样本内 top-10"∩原生 GT | **83.90%**（期望 84.49%） |
| 用 **全量基底** | **100.00%** |

**⇒ 那个 84.85% 是 `compat_probe_scaling.py` 的 `S_LIST` 上限（1e6）< `n`（1,183,514）造成的子采样伪影**
（子样本漏掉了 15.5% 的基底，真近邻有相应概率落在样本外）。
**U14 撤回**；同时**修正 P2 表的一处口径标注错误**：glove100 那一行的"S=全量"实际只是 **84.5% 子采样**。
⇒ 已修脚本（新增"基底占比"列并允许把 `n` 本身作为一个 S），并在 `p2-revised-result.md` 的限制里更正。

---

## 3. N3 — 论文原文核对（§6 + 结论）**强化**了我们唯一的新贡献

逐字要点（抓取自 arXiv HTML v3）：

| 问题 | 论文的答案 | 对我们是 |
|---|---|---|
| 探针定义在哪 | **§6（Analysis: The Impossible Triangle）**，不在 §5.6 | — |
| 用哪个 BQ 度量 | **未指定**（全文默认 2-bit SM 的加权 6 类，但探针段落没写） | ✅ 我们的"度量歧义"批评成立 |
| 样本量 | "∼10K vectors" | ✅ 与我们的 S 扫描对话 |
| 样本来源（是否含查询） | **未说明** | ✅ 我们的实现选择是合理的一种 |
| K | **top-10** | ✅ 与我们一致 |
| 阈值 | **>∼50% 兼容 / <∼50% 用 float32** | ✅ 与我们一致 |
| **是否提出任何预处理** | **完全没有**。卖点是 "**zero preprocessing**"、"no codebook or rotation training"；并把 RaBitQ 的旋转描述为 "an O(D²) preprocessing step" | ★★ **我们的去均值修复落在其空白处** |
| 对失败数据的建议 | **"float32-based indexing is the safer choice"** | ★★ 我们给出**第三条路**：一次 O(D) 数据的去均值，让 BQ-native 索引继续可用 |

⇒ **论文把"符号位失效"写成了症状与分档，但没有给出修复**（N3 之前我们担心"机制已发表、无新意"；
现在可以说：**机制已发表、但"可修复"这件事及其量化效果没有**）。

---

## 4. N4 — 竞品地图：**QuIVer 只在 1 个档位赢**

### 4.1 四档 × 竞品（本轮补齐 CLIP 档与可用档）

| 论文档位 | 代表数据集 | QuIVer 最高（@ef_s=1024） | 最强竞品 | 判定 |
|---|---|---|---|---|
| **竞争 >88%** | cohere-768 | 99.78% | hnswlib 99.84% @741 QPS | ✅ **QuIVer 同召回快 4.6–5.0×** |
| 较高 71–78% | **wolt_clip-512** | **86.81%** | hnswlib **87.86% @19,606**（ef=64） | ❌ **严格支配**（QuIVer 的上限 < HNSW 的**最低**工作点） |
| 可用 32–42% | **glove100-100** | **71.69%** | hnswlib **82.05% @36,047**（ef=64） | ❌ **严格支配** |
| 崩塌 <15% | sift128-128 | 47.21% | hnswlib 97.46% @45,118（ef=64） | ❌ **严格支配** |
| 崩塌 | gist960 / sphere | 4.38% / 6.46% | *未测*（推测同样支配） | 崩塌 |

**判定方式不需要 QPS**：在上面三个档位里，**QuIVer 扫描到 ef_s=1024 的上限仍低于 HNSW 在 ef=64 的召回**
⇒ 竞品曲线与 QuIVer 曲线**没有交点** ⇒ 支配与速度无关。

### 4.2 新发现：`wolt_clip` 上 **`faiss_exact` 只有 90.09%**
精确 f32 暴力搜索在 wolt_clip 上只复现 **90.09%** 的官方 GT（cohere `100.00%`、glove100 `100.00%`、
sift128 `99.94%`）。合理解释是**密集并列**（该集拥挤度中位 151、均值 8751；CLIP 近重复图片很多）
⇒ **该数据集的召回有一个 ~10pp 的"并列地板"**：任何方法（含 HNSW 的 89.89%）都到不了 100%。
⇒ 报 wolt_clip 的召回时**必须**同时报 `faiss_exact` 的值，否则会误读。

---

## 5. 旧结论审查（逐条裁决）

| # | 旧结论 | 裁决 | 依据 |
|---|---|---|---|
| A1 | 组织性：`src/` 零改动、研究 harness 与论文 harness 等价 | ✅ **成立（加强）** | 校准从 6/12 升到 **7/12**，内部分歧 ≤1.84pp |
| A2 | cohere-768 上 QuIVer 同召回快 4.1–5.0×、建图 5.3× | ✅ **成立** | 四个实现一致；本轮无变化 |
| A3 | **gist960 崩塌的机制 = 全非负 ⇒ pos 位面零信息 ⇒ weighted 度量退化为按 `|h|` 选邻居** | ✅ **成立（且是论文 Finding 1 的同款结论）** | 解析恒等式 + 偏置 +3.34σ + 图保真度 0.57% |
| A4 | 图保真度 `L0∩cos_top64`（gist960 0.57% / cohere 56.84%）是码侧探针看不见的轴 | ⚠️ **成立但只在 2 个数据集上测过** | 仍是本轮最强的方法学增量；需补 1–2 个数据集 |
| A5 | **去均值修复**（gist960 +49pp） | ✅ **成立并大幅加强**：全档位表 + **无操作对照 +0.03pp** | §1.2 |
| A6 | 两个结构缺陷（建图/查询度量不一致；α=1.2 在平台最差端） | ✅ **成立（代码事实 + 实测）** | 未变 |
| A7 | **"用可分性预测边界"** | ❌ **已撤回** | `sift128c` 反例（可分性降低而召回翻倍） |
| A8 | **"码估计信噪比 SNR 可作单一预测器"** | ❌ **已撤回（P1 证伪）** | ρ=+0.30（含 Random-Sphere 臂） |
| A9 | **P2"崩塌档可被 resolved 进可用/竞争档"** | ⚠️ **需修正** | **修复只提高召回，不改变竞争判定**：gist960c 上限 79.4% / glove100c 73.3%，仍 **< HNSW 的最低工作点**（§4.1） |
| **A10** | **U14「glove100 GT 口径模糊」** | ❌ **撤回（N2）** | 是**我的子采样伪影** |
| **A11** | **"论文的四档是适用性梯度，可用于部署判断"** | ⚠️ **需重大修正（N4）** | 四档描述的是**绝对召回**，不是**竞争力**；有竞品数据的 3/4 档都被 HNSW 支配 ⇒ 部署判断应改为"**只有最高档 QuIVer 才有优势**" |

**审查的净效果**：技术结论（A1–A6）站得住，且 A5 被显著加强；**两个预测器尝试（A7/A8）都已撤回**；
**两处需要修正的过度表述（A9/A11）**恰恰是最容易写成"论文卖点"的两处 —— 必须先改。

---

## 6. 推导新方向

### D1（推荐）—— 把"能不能用"变成**几秒钟可算的判据 + 一条修复路径**
现有的全部零件可以合成一条**部署决策链**（每一步都可预先计算，都不需要建索引）：

```
① 符号平面是否活着？  逐坐标负值占比（或 pos 面 Hamming 率）  ── ≈0 ⇒ ② 否则跳到 ③
② 修：x' = normalize(x − μ)，μ = mean(normalized train)，O(D) 状态  ── 修后重算 ①
③ 任务是否可解？      (GT10−GT11) / cos_std（可分性）与拥挤度  ── 太小 ⇒ 换 float32 索引
④ 会不会真赢？        在 ~10K 样本上算码 top-10 命中率（min 双度量版本，ρ=0.95） ── <50% ⇒ 用 float32
```
**为什么这是最强的方向**：它直接缝合了论文的三个空白（未指定度量 / 未提预处理 / 把失败归为数据类不可用），
且**每一步我们都有实测支撑**（§1.2、P2、N3）。它也是**可被第三方在几分钟内验证**的（这是审稿/复现友好性）。

### D2 —— 把"修复后是否变强"追问到底（**这是 A9/A11 的兜底**）
必须把 `wolt_clipc / gist960c / glove100c` 也跑竞品曲线，回答"修复后是否进入支配区之外"。
**若答案仍是否**（目前趋势如此），论文的主张就必须写成"**修复让索引可用，但不改变与 HNSW 的胜负**"——
这仍然是可发表的诚实结论，**但必须现在就接受它**，不能等到写作时才发现。

### D3（不推荐，高成本/高风险）—— 新估计器（Tier C）
我们知道 `distance_weighted = 4d − <w_a,w_b>`，且偏差来自 `|h_b|`。
**理论上**可以做一个"去偏的 BQ2 估计器"（解析地扣掉 `|h|` 的期望项）并给误差界 ——
但那与 **RaBitQ 的估计器理论同源**，很难在顶会站住；而且 T1（RaBitQ 剪枝）已实测失效。
⇒ 除非决定投 Tier C，否则不要启动。

### D4（低成本高价值，与论文无关）—— 上游 issue/PR
本轮又添两条可交付的缺陷：① `README_QUIVER.md` 的 Table 编号错（Table 10/Figure 4 ↔ 实为 Table 11/Figure 3）；
② `scripts/prepare_all.py` 的 `binary_vector` 分支假设 JSON 字符串，当前 wolt_clip revision 直接给 `list`
⇒ `TypeError`（已最小容错修复）。加上 F1（度量不一致）与 α 默认值，共 4 条。

---

## 7. 论文可行性

### 7.1 若今天动笔，这篇论文的"可选贡献"清单

| # | 贡献 | 强度 | 是否已在论文里 |
|---|---|---|---|
| C1 | **去均值修复**：让"崩塌档"从 `2.01%` 到 `39.74%`（@ef=64），CLIP 档 +5.3pp，cohere 中性，各向同性数据 +0.03pp（无操作对照） | ★★★ | ❌ **论文明确"zero preprocessing"且建议改用 float32** |
| C2 | **竞争力边界 ≠ 适用性梯度**：有竞品数据的 3/4 档被 HNSW 严格支配（含论文称"较高"的 CLIP 档） | ★★★ | ❌ 论文只报绝对召回 |
| C3 | **判据的两处不完备**：未指定度量（Synthetic-LR 上两度量结论相反）、强依赖样本量（6.8×） | ★★☆ | ❌ |
| C4 | **可预先计算的诊断链**（符号平面 + 可分性 + 码命中率） | ★★☆ | 部分（论文只有末项且未指定度量） |
| C5 | 两个工程缺陷（度量不一致 / α 默认值） | ★★ | ❌ |
| C6 | 复现性：7/12 行、4–5× 加速、`faiss_exact` 并列地板 | ★★ | — |

### 7.2 仍缺（写论文的硬缺口）

| # | 缺口 | 成本 |
|---|---|---|
| G-a | 论文剩余 5 行数据集（`minilm-384` / `bge-m3-1024` / `dbpedia-1536/3072`；`redcaps-512` 可能不可得） | 1–2 天（下载+建图+探针） |
| G-b | **修复后的竞品曲线**（`wolt_clipc` / `gist960c` / `glove100c`）——D2，决定 A9/A11 的措辞 | 0.5–1 天 |
| G-c | 图保真度轴补到 ≥6 个数据集（现在只有 4 个） | 0.5 天 |
| G-d | F1 补丁端到端（**需批准改 `src/`**）+ 代价（本机无 AVX-512） | 0.5–1 天 |
| G-e | 第二台 AVX-512 机器复测 4–5× 倍差（倍差是 SIMD 敏感的） | 0.5–1 天 |
| G-f | 统计：关键点 ≥3 次重复 + G-det 声明 | 0.5 天 |

### 7.3 结论与建议

> **值得继续，但要把目标锁定在 Tier A，并且现在就必须接受"A9/A11 修正后的口径"。**

- **继续的理由**：C1+C2+C3 是**互相独立、都已实测、且都对着论文的具体主张**的三条；
  D1 的决策链是**可被第三方几分钟内验证**的实用产物；G-a~G-f 全部是**有界**的机械工作量（1–2 周）。
- **不建议追顶会**：机制（Finding 1）已发表；我们的两次"新预测器"尝试都已证伪；
  修复是数据预处理 + 实现级补丁 ⇒ **没有可证明的新技术**。顶会需要 Tier C（D3），那是另一个项目。
- **venue 优先级**：① 上游 issue/PR（现在就做，无风险）；② arXiv 技术报告 / 复现类短文（Tier A）；
  ③ workshop（若 C2 的竞品地图足够完整）。
- **停止条件（明确写死，避免无限拉长）**：
  1. G-b 若显示"修复后在**任何**数据集上 QuIVer 都进不了支配区之外" ⇒ 论文主张降级为"**狭窄的负面+修复**"，**不再追加数据集**（G-a 只补 2 个）；
  2. G-e 若拿不到 AVX-512 机器 ⇒ 全篇改口径为 single-platform，**不再等**；
  3. 累计投入超过 **2 周**仍未完成 G-a~G-d ⇒ 只发 arXiv 技术报告 + 上游 PR，停止。

---

## 8. 限制与风险（本轮新增）

| # | 项 | 说明 |
|---|---|---|
| L1 | **wolt_clip 的并列地板** | `faiss_exact = 90.09%` ⇒ 该集召回不可与其它集直接横比；任何方法的上限 ~90% |
| L2 | **去均值改变相似度定义** | §1.2 的 Δ 是"**在 centered-cosine 任务上**的召回"与"原任务召回"之差，**不是同一任务的提升**。论文中必须显式区分（本文档与 `t2-gist960-collapse.md` 都已标注） |
| L3 | ~~**`wolt_clip` 实际规模**：流式只取到 `953,662` 训练行~~ ❌ **本条是我自己算错**（把 MiB 当 MB）：`wolt_clip_train.f32` = 2,048,000,000 B = **1,000,000 × 512 行**、test = **1000** 行，与论文的 1M/1K **完全一致**，故 +0.80pp 的对齐**不含规模差异** |
| L4 | **N4 未测** | `gist960 / sphere / random` 的竞品曲线；`*c` 修复臂的竞品曲线 |
| L5 | 单机、无 AVX-512 | 倍差是 SIMD 敏感的；QuIVer 吃 VPOPCNTDQ，HNSW 不吃 |
| L6 | 论文数字仍未与 PDF 逐格复核 | 抓取转述 |
| L7 | `prepare_all.py` 已被最小修改（`binary_vector` 容错） | 非 `src/`；已记入上游缺陷清单 |

---

## 9. D2 —— 修复臂的竞品曲线（**修正 A9/A11**）

在**去均值后**的数据上重跑竞品（GT 同为 centered-cosine，口径一致）。

### 9.1 `wolt_clipc`（centered CLIP-512）：**曲线相交 ⇒ 不再被支配**

| R@10 | QuIVer（ef_s / MT-QPS） | hnswlib（ef / MT-QPS） |
|---|---|---|
| ~82.3% | ef_s=128 / **59,499** | — |
| ~87.6–87.9% | ef_s=512 / **18,299**（87.61%） | ef=64 / **20,849**（87.88%） |
| ~89.0–89.1% | ef_s=1024 / **9,356**（89.05%） | ef=128 / **12,350**（89.09%） |
| ~89.8–90.1% | —（上限 89.05%） | ef=512 / 4,124（89.79%）、ef=1024 / 2,394（**90.07%**） |
| 参考 | — | `faiss_exact` = **90.18%**（并列地板，与去均值前 90.09% 同） |

**读数**：低 ef 区 HNSW 快 1.1–1.3×；**≥89% 的高召回区 QuIVer 快 2.3–3.9×**（9,356 vs 4,124 / 2,394）。
**去均值前**：QuIVer 上限 86.81% **< HNSW 的 ef=64（87.86%）** ⇒ 曲线无交点 ⇒ **严格支配**。
⇒ **去均值把该档从"严格支配"变成"互有胜负的相交"**。

### 9.2 `glove100c`（centered GloVe-100）：**仍严格支配**

| | QuIVer 上限 | hnswlib ef=64 | 判定 |
|---|---|---|---|
| 去均值前 | 71.69% @11,517 | **82.05% @36,047** | 支配 |
| 去均值后 | **73.31% @11,021** | **83.98% @33,617** | **仍支配**（召回与 QPS 双双更低 ⇒ 无交点） |

### 9.3 `gist960c`：**未测**（HNSW 在 GIST 上按其它集推断约 99%，QuIVer 上限 79.44% ⇒ 预期仍支配）

### 9.4 ★ 对 A9/A11 的口径修正

| # | 原表述 | **修正后** |
|---|---|---|
| **A9** | "去均值**只提高召回，不改变竞争判定**" | ⚠️ **过于绝对**：去均值把 **CLIP 档从"严格支配"推入"相交/互有胜负"**（高召回区 QuIVer 快 2.3–3.9×）；**崩塌档与可用档的判定不变**（仍被支配） |
| **A11** | "有竞品数据的 3/4 档被 HNSW 严格支配 ⇒ 只有最高档 QuIVer 才有优势" | 修正为：**"去均值前 3/4 档被严格支配；去均值后 CLIP 档进入相交区，严格支配的只剩崩塌档与可用档"** ⇒ 竞争力边界比先前结论**略宽**，但**明确获胜的仍只有最高档（cohere-768，4.6–5.0×）** |
| C1 的价值 | "修好可用性" | **升级**：修好可用性 **+ 把一个档位从被支配推到互有胜负**（这是 §7.1 里 C1 强度从 ★★★ 更稳的依据） |

**限制**：wolt_clip 系两集都被**并列地板**限住（`faiss_exact` 90.09/90.18%）⇒ 两者都挤在天花板附近，
"相交"结论部分由并列造成；`gist960c` 未测；单机无 AVX-512。

---

## 10. 交付物

| 类型 | 路径 |
|---|---|
| 本文 | `docs/research/audit-and-direction.md` |
| D1 | `docs/research/t2-deployability-gate.md` + `scripts/research/deployability_gate.py` + `results/t2/deployability_gate.json` |
| 新数据集 | `wolt_clip_*`（HF 下载）+ 全档位去均值臂 `{coherec, wolt_clipc, glove100c, randomc, spherec, gist960c, sift128c}_*` |
| 脚本改动 | `gist960_collapse_prepare.py`（新增 `--center PREFIX:DIM` 通用去均值 + `‖μ‖≤1` 守卫）、`prepare_all.py`（`binary_vector` 容错） |
| 日志 | `.tmp/n1_*.log`、`.tmp/n2*.log`、`.tmp/n4_bl_*.log` |
| `src/` 改动 | **无** |
